{
  "name": "@fastr-docs/mermaid-frame",
  "private": true,
  "version": "0.0.0-dev",
  "description": "Read-only Mermaid renderer that runs inside the opaque-origin sandboxed iframe and reports its height to the host.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "mermaid": "11.17.2"
  },
  "devDependencies": {
    "esbuild": "0.25.10"
  }
}
